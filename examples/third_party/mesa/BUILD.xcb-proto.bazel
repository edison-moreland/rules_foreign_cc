load("@rules_foreign_cc//foreign_cc:defs.bzl", "configure_make")

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "all_srcs",
    srcs = glob(
        include = ["**"],
        exclude = ["*.bazel"],
    ),
)
# TODO put the hermetic python on the path
# add lib dir as an out_data_dir

configure_make(
    name = "xcb-proto",
    lib_source = ":all_srcs",
    out_data_dirs = ["lib"],
    out_headers_only = True,
)
